# FSOC ML/CV Project Review - September 9, 2026

This review tracks Phase 8 work: adapting the first Unity-generated continuous sequence and running an offline baseline evaluation.

## Starting State

Repository:

```text
https://github.com/sidnim12/fsoc-ml-cv
```

Branch state before edits:

```text
main tracks origin/main
working tree clean
```

Virtual environment:

```powershell
.\.venv\Scripts\python.exe --version
```

Result:

```text
Python 3.14.1
```

Baseline tests before Phase 8 edits:

```powershell
.\.venv\Scripts\python.exe -m pytest -v
```

Result:

```text
33 passed
```

## Phase 8 - Unity Dataset Adaptation And Baseline Evaluation

Implemented:

```text
src/unity_dataset_adapter.py
src/evaluate_unity_sequence.py
tests/test_unity_dataset_adapter.py
tests/test_unity_evaluation.py
```

Updated:

```text
requirements.txt
README.md
notebooks/03_dataset_analysis.ipynb
review/progress_2026-09-09.md
```

Added dependency:

```text
openpyxl>=3.1
```

Reason:

Phase 8 requires CSV and XLSX label support. `openpyxl` was not installed in the project environment, so it was added to `requirements.txt` and installed into `.venv`.

## Unity Sequence Discovery

Discovered sequence:

```text
data/raw/unity/smooth_horizontal01/sequence_001
```

Inventory:

```text
Images: 300
Format: .png
Filename pattern: frame_{frame:06d}.png
First frame: 0
Last frame: 299
Resolution: 640 x 480
Channels: 3
Labels: data/raw/unity/smooth_horizontal01/sequence_001/labels.csv
```

The user asked earlier about `sequence001`; the actual folder and labels use `sequence_001`.

## Dataset Validation

Validation output:

```text
Expected image count: 300
Actual image count: 300
Frame numbers unique: true
Frame numbers continuous: true
Missing frame numbers: 0
Unreadable images: 0
Resolution mismatches: 0
Duplicate image files: 0
All frames empty: false
Unity Editor UI suspected: false
Label image references valid: true
Usable: true
```

Brightness range:

```text
Minimum mean brightness: 9.353018
Maximum mean brightness: 15.738969
Mean brightness: 13.22997
```

The original Unity images were not copied, resized, moved, renamed, overwritten, or used for training.

## Normalized Manifest

Created:

```text
data/processed/unity/smooth_horizontal_01/manifest.csv
outputs/unity-evaluation/smooth_horizontal_01/normalized_manifest.csv
```

The manifest stores paths to the original Unity images. It does not duplicate image data.

Coordinate origin used for this run:

```text
top-left
```

A quick sanity check showed top-left label interpretation gives better candidate recall than bottom-left interpretation:

```text
top-left candidate recall: 29 / 300 = 0.096667
bottom-left interpretation candidate recall: 15 / 300 = 0.05
```

## Evaluation Command

Command used:

```powershell
.\.venv\Scripts\python.exe src\evaluate_unity_sequence.py --sequence-dir data\raw\unity\smooth_horizontal01\sequence_001 --config configs\default.yaml --checkpoint models\checkpoints\best_classifier.pt --fps 30 --coordinate-origin top-left --output outputs\unity-evaluation\smooth_horizontal_01 --expected-count 300 --expected-width 640 --expected-height 480 --device cpu
```

Important constraints followed:

```text
CNN checkpoint loaded once
Tracker created once and reset only before frame 0
Frames processed in frame-number order
No frame shuffling
No future-frame information used
Prediction columns not treated as ground truth
No CNN retraining
No API, PID, ONNX, or live Unity networking
```

## Baseline Detection Metrics

Output:

```text
outputs/unity-evaluation/smooth_horizontal_01/detection_metrics.json
```

Result:

```text
Visible target frames: 300
Target-absent frames: 0
Candidate recall: 0.096667
Accepted-detection recall: 0.0
Missed detections: 300
False-positive frames: 0
False-negative frames: 300
Target-found rate: 0.563333
Match tolerance: 12 px
```

Interpretation:

The detector and CNN do not yet transfer well from Python-generated training images to this Unity sequence. The pipeline often detects bright candidates, but they are usually not the labelled beacon.

## Baseline Tracking Metrics

Output:

```text
outputs/unity-evaluation/smooth_horizontal_01/tracking_metrics.json
```

Result:

```text
Time to first lock: 2 frames / 0.066667 s
Locked-frame percentage: 84.333333
Acquiring-frame percentage: 1.333333
Coasting-frame percentage: 10.666667
Lost-frame percentage: 0.333333
Searching-frame percentage: 3.333333
Number of lock events: 16
Number of lost-lock events: 1
Number of reacquisitions: 0
Maximum consecutive missed frames: 9
```

Coordinate metrics:

```text
Measurement MAE: 270.328483 px
Filtered MAE: 288.899709 px
Measurement RMSE: 312.944174 px
Filtered RMSE: 329.319087 px
Measurement max error: 512.149443 px
Filtered max error: 525.388316 px
Measurement median error: 329.19227 px
Filtered median error: 344.542363 px
Measurement 95th-percentile error: 499.303229 px
Filtered 95th-percentile error: 509.30238 px
```

Smoothness metrics:

```text
Raw measurement jitter: 7.013547
Filtered-position jitter: 3.44471
Jitter reduction: 50.884909%
```

Interpretation:

The Kalman tracker smooths the selected detections, but since the selected detections are usually the wrong bright object, the smoothed track is also wrong relative to Unity ground truth.

## Performance Metrics

Output:

```text
outputs/unity-evaluation/smooth_horizontal_01/performance_metrics.json
```

Result:

```text
Mean processing time: 51.591933 ms/frame
Median processing time: 42.699 ms/frame
95th-percentile processing time: 94.37445 ms/frame
Maximum processing time: 268.197 ms/frame
Effective processing FPS: 19.382875
Frame count: 300
```

## Domain-Gap Report

Output:

```text
outputs/unity-evaluation/smooth_horizontal_01/domain_gap_report.json
```

Result:

```text
Average correct-beacon probability across all candidates: 0.112807
Selected average correct-beacon probability: 0.581765
Minimum probability: 0.000002
Maximum probability: 0.999893
Candidates above confidence threshold: 0.088623
Average candidates per frame: 8.086667
Frames with zero candidates: 0
Frames with multiple candidates: 300
Most common failure reason: selected_candidate_far_from_ground_truth
Failure counts:
  below_confidence_threshold: 131
  selected_candidate_far_from_ground_truth: 169
```

Representative first-frame behaviour:

```text
Ground truth: (320.0, 240.0)
Selected candidate: approximately (569.9, 478.0)
Candidate count: 6
Failure reason: selected_candidate_far_from_ground_truth
```

## Generated Outputs

Created:

```text
outputs/unity-evaluation/smooth_horizontal_01/dataset_validation.json
outputs/unity-evaluation/smooth_horizontal_01/normalized_manifest.csv
outputs/unity-evaluation/smooth_horizontal_01/frame_predictions.csv
outputs/unity-evaluation/smooth_horizontal_01/detection_metrics.json
outputs/unity-evaluation/smooth_horizontal_01/tracking_metrics.json
outputs/unity-evaluation/smooth_horizontal_01/performance_metrics.json
outputs/unity-evaluation/smooth_horizontal_01/domain_gap_report.json
outputs/unity-evaluation/smooth_horizontal_01/evaluation_summary.json
outputs/unity-evaluation/smooth_horizontal_01/annotated_tracking.mp4
outputs/unity-evaluation/smooth_horizontal_01/trajectory_comparison.png
outputs/unity-evaluation/smooth_horizontal_01/confidence_over_time.png
outputs/unity-evaluation/smooth_horizontal_01/coordinate_error_over_time.png
outputs/unity-evaluation/smooth_horizontal_01/lock_state_timeline.png
outputs/unity-evaluation/smooth_horizontal_01/failure_montage.png
```

Output file sizes:

```text
annotated_tracking.mp4: 1885186 bytes
frame_predictions.csv: 1398501 bytes
failure_montage.png: 320983 bytes
```

Generated output files are ignored by Git. Only `.gitkeep` placeholders are tracked for the output folder structure.

## Annotated Video Overlay Fix

Regenerated:

```text
outputs/unity-evaluation/smooth_horizontal_01/annotated_tracking.mp4
```

What changed:

```text
Moved metrics into a dynamic corner panel that avoids the beacon/track points
Moved the legend into a compact readable corner panel
Changed non-selected candidate rings from yellow to neutral gray
Made the selected-candidate trail bright yellow and easier to see
Added separate trails for selected candidate, filtered track, and Unity ground truth
```

Visual interpretation:

```text
Red circle/trail: Unity ground-truth beacon
Yellow circle/trail: selected ML/CV candidate
Blue trail / F marker: Kalman-filtered track
Green M marker: measured selected candidate
Magenta P marker: tracker prediction
Gray rings: other bright candidates detected by OpenCV
```

The yellow selected trail appearing near the Earth edge is not an overlay placement bug. It confirms the current Unity-domain failure: the detector/classifier is often selecting bright Earth-edge artifacts instead of the labelled beacon.

## Tests

Phase 8 targeted tests:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_unity_dataset_adapter.py tests\test_unity_evaluation.py -v
```

Result:

```text
19 passed
```

Full project tests:

```powershell
.\.venv\Scripts\python.exe -m pytest -v
```

Result:

```text
52 passed
```

## Next Work Before CNN Fine-Tuning

Do not fine-tune on this single sequence alone.

Before CNN fine-tuning:

```text
Collect multiple independent Unity sequences
Include clean, stars, false beacons, noise, blur, target absent, fade, occlusion, and disturbance cases
Confirm coordinate origin and label export from Unity
Review the bright bottom/right artifacts that dominate current selections
Tune detector thresholds separately from CNN training
Hold out at least one full sequence for honest evaluation
```
